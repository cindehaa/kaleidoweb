# Next

## Blocked on account spend limit (agents dying at spawn; raise at claude.ai/settings/usage)
- [ ] Apollo v4 — the editorial cut: EDITORIAL.md's worked allocation (surface 94.6% → ~34%, 10 named cuts, label purge). Agent died at spawn; brief is ready in EDITORIAL.md §method.
- [ ] Hokusai critique panel (two-lens: rubric #1–17 audit + cold reader) — died at spawn. Then its fix round + material re-derivation (ANTI_SIGNATURE worked example 2: key-block boundary, flat opaque colour, stamped seal register, one vertical-axis moment).

## Done this cycle
- [x] Apollo v3 material re-derivation (white bond / Jost / Courier Prime / ruled cells) — sibling test vs Hokusai passes; re-bundled into extension
- [x] Hokusai "Nothing Before Seventy" v1 (12 iterations) + collision fixes (margin opacity, name ticks)
- [x] Principles v0.3 (anti-signature + editorial amendments, 16 pre-ship checks); counter-canon added to INSPIRATION.md

## After unblocking
- [ ] Bundle Hokusai as second demo once critiqued; anti-genericness side-by-side record
- [ ] Latency measurements on demo path (button → first paint → coherent)

## Then
- Third benchmark (radically different again: Fourier transform — interactive math — or Silk Road — geographic journey)
- Begin abstraction pass ONLY after 3 subjects prove the ceiling: what do the three Experience Specs share? → declarative spec draft
- Runtime pipeline design: live extraction (works) → cheap-model semantic pass → strong-model art direction → spec interpreter in extension
- Regenerate/reinterpret affordance in extension UI; API-key settings page
